import { ResetAutosPipe } from './reset-autos.pipe';

describe('ResetAutosPipe', () => {
  it('create an instance', () => {
    const pipe = new ResetAutosPipe();
    expect(pipe).toBeTruthy();
  });
});
