import { AutosReducidoPipe } from './autos-reducido.pipe';

describe('AutosReducidoPipe', () => {
  it('create an instance', () => {
    const pipe = new AutosReducidoPipe();
    expect(pipe).toBeTruthy();
  });
});
